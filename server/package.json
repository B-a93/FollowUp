{
  "name": "followup-api",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npm install --prefix .. --ignore-scripts && npm --prefix .. run build",
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit && esbuild src/index.ts --bundle --platform=node --format=esm --target=node22 --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" --outfile=dist/index.js && esbuild src/migrate.ts --bundle --platform=node --format=esm --target=node22 --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" --outfile=dist/migrate.js && cp -R sql dist/sql",
    "start": "node dist/index.js",
    "migrate": "tsx src/migrate.ts",
    "migrate:prod": "node dist/migrate.js"
  },
  "dependencies": {
    "bcryptjs": "latest",
    "cors": "latest",
    "dotenv": "latest",
    "express": "latest",
    "jsonwebtoken": "latest",
    "pg": "latest",
    "nodemailer": "latest",
    "@types/cors": "latest",
    "@types/express": "latest",
    "@types/jsonwebtoken": "latest",
    "@types/node": "latest",
    "@types/pg": "latest",
    "@types/nodemailer": "latest"
  },
  "devDependencies": {
    "esbuild": "latest",
    "tsx": "latest",
    "typescript": "latest"
  }
}
